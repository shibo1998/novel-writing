<!-- SOLOENT-STATE
chapter: 0
story_time: 
location: 
narrative_verified: 0
updated: YYYY-MM-DD
-->

> ↑ 上面这个状态块是**机器可校验**的（`python tools/state_sync.py`）。
> 字段清单由 `.soloent/book.json` 的 `state.required` 定义，**两处必须一致**。
>
> **怎么填**：
> - `chapter` —— 可机器同步。跑 `python tools/state_sync.py --fix` 会自动对齐。
> - 其余字段 —— 叙述性字段，**必须人工/AI 回写**成该章结束时的真实状态。
> - `narrative_verified` —— **人工确认戳**：把叙述性字段都补好后，改成本章号。
>   `--fix` **不会碰它**，所以跑 `--fix` 无法绕过这道（防「章节号修好了、状态其实是旧的」）。
> - 三者必须一致，否则 `tools/preflight.py` 拦截。
>
> 本文件是 SOLOENT.md §7「活跃写作状态」的**详细版**；SOLOENT.md 里只放摘要。
> **两者冲突时以本文件的状态块为准，并须立刻修正 SOLOENT.md。**

## 流程留痕（正文步骤 2–5（SKILL B3–B6），每章一行；闸门校验，缺则拦截）

<!-- 每写完一章追加一行，删掉下面这行示例 -->

`[流程] ch-1 自查0 三连0 复查0 回写OK`

> 格式：`[流程] ch-N 自查X 三连Y 复查Z 回写OK`
> X／Y／Z ＝ 该步**发现并修复**的处数（干净章写 0 是正常的，但必须是真跑过的）。
> 为什么要有这一行：闸门能验人名／刻度／字数／账本，**验不了 SKILL B3 句法自查／B4 技能三连／B5 连续性复查跑没跑**；`回写OK` 对应 B6 的收尾回写。
> **缺这一行 → `tools/preflight.py` 直接拦截。**
>
> ⚠️ **闸门还会把它与本章的机检残留对撞**（2026-09-20 起）：本章仍有黑名单词／句式禁项／
> 同段连发／比喻堆砌／章末升华／面板条目残留，又没说保留理由 → **同样拦截**。
> `自查0 三连0 复查0` 只保证格式合法，不代表真跑过。**确需保留的违规处**写成：
> `[流程] ch-N 自查X 三连Y 复查Z 回写OK；保留：<理由>`（降为警告，/review 逐条复核）。

---

# 活跃写作状态

> 仪表盘：在开始写第一章之前、以及**每章完成后**更新。

## 当前状态

*我们现在在哪里？*

- **当前章节**：第 0 章（尚未开写）
- **故事时间**：
- **当前地点**：
- **即时氛围**：

## 人物快照

*谁在这里，他们怎么样？（只列出活跃/有变化的人物）*

- **_（角色）**：

## 近期情节流

*刚刚发生了什么？（只保留最近 3 章）*

- **第 X-2 章**：
- **第 X-1 章**：
- **第 X 章**：

## 待处理线索

*接下来需要处理什么？*

- **紧急钩子**：
- **活跃伏笔**：
- **下一章目标**：

## 执行评审

*最新章节的质量检查。*

- **字数**：实际 / 目标
- **大纲符合度**：是 / 否（是否严格按细纲执行？）
- **范围控制**：通过 / 未通过（是否避免写进下一章的情节？）
- **闸门**：严重 N ｜ 中等 N ｜ 轻微 N ｜ 流程留痕 ✅

## 数值账目

> 以 `.soloent/ledger.tsv` 为**唯一事实来源**，这里只放给人看的摘要。

- _（如：潜力点 22 ｜ 天赋石 110 ｜ 现金 277.5）_
