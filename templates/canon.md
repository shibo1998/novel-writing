# 正典速查表 · 单一来源

> **写作前必读，关键值一律以此表为准，不凭印象。**
> 本表是**人类可读**的正典；机检用的结构化版本在 `.soloent/book.json` 的 `checks` 区块。
> **改一处必须同步另一处——两处不一致 = 机制失效。**
>
> 登记纪律：写作时新造的专有细节（人名／地名／物象存放位置／数值口径／时间锚点）
> **当章登记到本表**，并同步加一条机检项。不登记 = 下一章很可能重造一个不一致的版本。

## 一、人物

| 角色 | 唯一写法 | 常见错写 | 定位 / 不能写成什么 |
|---|---|---|---|
| ✏️主角 | | | |
| ✏️ | | | |

> 一人一卡：详细人设放 `characters/`，本表只放**易错项**（名讳、称谓、年龄、关系）。

## 二、数值与刻度

| 项 | 正典值 | 说明 |
|---|---|---|
| ✏️境界/评级阶梯 | | |
| ✏️每日收益 | | |
| ✏️升级成本 | | |

## 三、时间轴锚点

| 事件 | 距今 | 对应年龄/年级 |
|---|---|---|
| ✏️ | | |

> 只登记**已被正文写过**的锚点。新锚点当章登记。

## 四、地点与专有细节（锁定写法）

| 细节 | 唯一写法 | 常见错写 | 首次出现 |
|---|---|---|---|
| ✏️ | | | |

> 这张表对应 `book.json` 的 `checks.locked_details`。**「同一件事各章各写一套」是最难发现的漂移**。

## 五、口径与禁令

- **口径**：✏️（如「振幅」用百分比制，不用自造单位）
- **情报禁令**（写到规定章位前不得给答案；埋线、暗示、让角色说错话都允许）：
  - 第 ✏️ 章前：
- **禁用词**：✏️

## 六、标点与格式

- 中文全角标点；正文对话一律用标准中文双引号（“ ”），严禁直角引号「」。
- 章标题格式：✏️（如 `# 第0XX章 标题`）

## 七、未解锁的未来设定（悬空闸）

| ID | 关键词 | 允许出现的章 | 解锁章 |
|---|---|---|---|
| ✏️ | | | 待作者拍板 |

> 对应 `book.json` 的 `checks.gated_keywords`。**拍板解锁后把该项第 5 个元素填成解锁章号，一行即可放行。**
