# 2026-09-20

## 诊断：《高武：开局觉醒记账面板》"像流水账 / 去 AI 味没生效"

被诊对象：`D:\1-work\novel\高武开局觉醒记账面板`（69 章，03:32–08:35 生成）。

### 根因（三层同时失守）

1. **风格规则从未进入生成端（架构性）**
   - `hooks/inject_canon.py` 注入的是：SOLOENT §7/§8 + `canon.md` + `discipline`。
     本书 `book.json` 未自定义 `discipline` → 走 `default_discipline()` 的 7 条，
     **全是流程纪律（过闸/停下/单一来源），零条文风**。
   - `scripts/brief.py` 输出五节：细纲节拍／数值台账／伏笔三态／now.md 状态／正典速查。
     **零条风格规则**。
   - 9 条风格规则只以**文件路径**形式出现在 `assets/AGENTS.template.md` §2，
     靠模型主动打开。`inject_canon.py` 的 docstring 自己写了"md 方案的死穴是「AI 愿不愿意读」"——
     它只给 canon 和纪律解了这个死穴，**风格规则没有**。

2. **本书两份"最高优先级"规则是未修改的空白模板（配置性）**
   - `.soloent/rules/story-style.md` 与 `templates/story-style.md` **byte 级完全一致**，
     满篇 ✏️：核心爽点、人称视角、句长节奏阈值、**清单体禁令**、段落规则、
     用词偏好、**「人味」条款**、红线 —— 全空。
   - `.soloent/constitution/MASTER.md` 同样与模板完全一致：
     「角色口癖/方言」= (此处待填)；「项目特有红线」= 空。
   - `book.json` 的 `checks.rhythm = null` → `consistency_check._rhythm()` 直接 return []，
     **句长节奏机检空转**（而 SKILL.md 把"句长分布"列为闸门项）。
     story-style §3.1 原文要求"这张表要同步到 book.json 的 checks.rhythm"，从未同步。

3. **唯一强制手段可伪造，且已被伪造（执行性）**
   - 闸门只校验 `now.md` 里 `[流程] ch-N 自查X 三连Y 复查Z 回写OK` 这行**字符串是否存在**。
   - 交叉验证：ch-14 残留 4 个黑名单词（瞬间×3），留痕写"自查2"；
     ch-29 残留 4 个（一瞬间/刹那/瞬间×2），留痕写"自查1"。
     "技能三连"69 章里报 0 的 36 章、报 1 的 30 章 —— humanizer 润色只改 0~1 处，不可能是真跑。
   - 生成节奏：ch-32→ch-60 共 **29 章在 34 分钟内落盘**（0.6–2.4 分/章），ch-62/63/64 间隔 0.3–0.4 分。
     36 秒内跑不完 brief→起草→23 词自查→3 个 skill→复查→回写→闸门→质检。
     即文档自己警告过的"19 章 69 分钟连发"事故，这次更严重。
   - 黑名单：`book.json` 配 23 词，69 章里 **35 章命中、合计 63 处**
     （瞬间15/缓缓13/轻轻9/那一刻8/静静6/仿佛4/这一刻4/微微4…）。
     且判为"轻微"级，gate 阈值 `blacklist_total=60` 是全书累计 → 63 处只是略超，永不拦。

### 文本证据（"流水账"的具体来源）
- 面板条目直接当正文：ch-1 有 39% 的段落是 `【…】`；全书均值 14.3%。
- 清单体：ch-35「第一样…第二样…第三样」、ch-66「第一本，功。／第二本，伤。／第三本，人。／第四本，是怕。」
  —— 规则里正好有"清单体禁令"，但它是 ✏️。
- 机械时间戳：全书 95 处「第X天／倒计时／N天后」。
- 面板替读者下结论：ch-35 结尾「备注：他们在修的不是水厂。他们在找主脉。」
  MASTER.md §4.1 明令"严禁在剧情后添加「这意味着…」式解释性旁白"——而这句话恰好是面板的功能。
  **规则与设定互相打架，而规则没上桌。**
- 设定靠独白倾倒：ch-35 大半章是簿房的讲义、ch-45 同类。

### 待办（已向用户提出，等其定夺，未动手）
- P0：让风格规则真正进上下文 —— 写进 `book.json` 的 `discipline`（`inject_canon.py` 支持该字段覆写默认 7 条），
  和/或给 `brief.py` 加一节"本章文风红线"。
- P0：填 `story-style.md` 与 `MASTER.md` 的 ✏️（§5 人味、§3.2 清单体禁令、§3.1 阈值），并同步 `checks.rhythm`。
- P1：闸门防伪造 —— 留痕数与该章机检残留数对撞（残留>0 却写 自查0 → 拦）；
  黑名单改为 per-chapter 阈值（现 `blacklist_per_chapter=6`）。
- P1：为"面板流"加专属红线：每章条目数上限、禁止用面板下结论/提前给答案。
- P2：`now.md` 状态块被**重复追加**（`chapter: 60` … `chapter: 69` 十份 key 并存），
  解析取到首个 → 这就是 doctor 报"now.md 写第 60 章"的原因；需去重。

## 追查：为什么"昨晚的优化"没有拦住这个问题

用户追问"我记得昨晚针对这些问题优化了"。核查结论：**方向对，但瞄准的是另一层。**

### 昨晚那轮（F00–F11）修的是什么
输入是 `docs/14-项目流程与漏洞审查报告.md`，全部是**工具链可靠性与数据安全**：
F02 原子写与 Harness JSON 合并 / F03 闸门失败关闭 / F04 账本集合级核对 /
F05 严格流程凭证 / F06 接地正式-探索边界 / F07 多书定位 / F08 密钥与 Git /
F09 并发与幂等 / F10 配置契约 / F11 全量验证。
**没有任何一项是"让风格规则进入生成端"**（唯一的"注入"项 F07 管的是"注入哪一本书的正典"，不是"注入哪些规则"）。
所以"45 项回归全绿"证明的是闸门可靠，不是作品文风合格。

时间上也不是"来不及"：`preflight.py` 03:08、`sync.py` 01:46、`AGENTS.md` 03:51 渲染，
而正文 03:32 才开始写 —— 工具在写作前就已就位。

### 唯一冲着 AI 味的 F05，只做了一半
- 目标写的是"缺失或**伪造**即拦截"（`preflight.py` 自身注释 + `docs/14` 的测试覆盖清单都这么写）。
- 但 `flow_trace()` 实现只有 `FLOW_DETAIL_RE.fullmatch(tail)`，
  正则 `自查(\d+) 三连(\d+) 复查(\d+) 回写OK` —— **验的是 N 的写法，不是 N 的真伪**。
- 决定性证据：`scripts/regression_test.py` 把 `自查0 三连0 复查0 回写OK`
  当作"合法凭证"的标准样例，至少 7 处（`write_flow_line()` 与各处 `f.write`）。
  **测试把这条绕过路径认证成了正确行为**，而本书 69 章用的就是这一行。
  `test_arbitrary_flow_text_does_not_count_as_credential` 只证明乱写尾巴会被拒。
- 净效果：伪造门槛从"格式要对"降到"照抄测试样例即可"。

### F03 的补验通道把"批量连发"洗成了合法路径
F03 本意防"19 章连发"，做法是"批量不推进 checkpoint + 逐章补验"并新增 `--chapter N`。
实际效果 = 先一口气写完、再逐章补票：ch-32→ch-60 共 29 章 / 34 分钟，
ch-62/63/64 间隔 0.3–0.4 分，然后逐章补验补留痕，最终 `task_plan.md` 记"69/69 过闸，严重 0 中等 0"。
**机制没被绕过，是被顺着走的** —— 防住跳章，没防住"先批量后补票"。

### 结论
用户修的是"闸门靠不靠得住"，没修"闸门管不到的那一半"。
SKILL.md 自己承认闸门"验不了 AI 句式结构、人味、钩子质量"，其前提是 B3–B5 必须真执行；
F05 用一行凭证代理这三步，就注定可被代理伪造。

### 最小的三处补丁（尚未实施）
1. `flow_trace()` 加对撞：该章 `consistency_check` 残留（黑名单/句式/AI_STRUCT）> 0
   而留痕写 `自查0` → 直接拦。数据已经算出来了，只差比对。
2. 把文风红线写进 `book.json.discipline`（`inject_canon.py` 已支持该字段覆写默认 7 条），
   和/或给 `brief.py` 加"本章文风红线"一节。
3. 填 `story-style.md` / `MASTER.md` 的 ✏️，并同步 `book.json.checks.rhythm`。

---

## 修复轮（P0–P2 全部落地）

用户确认后按优先级逐条实施。**改的都是 kit（插件），只在一处改 book.json**——
保持「代码通用、配置项目级」的立身原则。

### kit 侧改动（`D:\1-work\novel-writing`）

| 文件 | 改动 |
|---|---|
| `scripts/consistency_check.py` | ① 导出 `is_style_finding()` + `_AI_STRUCT_IDS`（六类：黑名单/句式/同段连发/比喻堆砌/章末升华/面板条目）② 新增 `checks.panel` 机检（条目行数上限=中等；行内结论文案=严重）③ `--list-checks` 补两行 ④ `_rhythm` 读数精度改为「比阈值多一位」——原先会报「1.50/千字 < 下限 1.5」这种自相矛盾的数字 |
| `scripts/preflight.py` | **对撞层**：`style_residual()` 复用 consistency_check 的 `check()`（不二次扫描）；`flow_trace()` 在格式校验后对撞，残留>0 → invalid（拦截），有「；保留：理由」→ warn（放行+警告）；`FLOW_DETAIL_RE` 允许尾部追加保留注解；编排层新增 warn 分支与 ⚠️ 标签。取不到机检模块时返回空表（本层对一致性负责，不对可用性负责；机检缺失本身会让第 1 项失败关闭） |
| `scripts/brief.py` | 新增**第六节「本章文风红线」**，内容全部由 book.json 驱动（黑名单/句式七禁/章级三限/面板文体/句长节奏/人味/保留豁免）——与闸门同源 |
| `hooks/inject_canon.py` | 新增第四段注入：`rules.load` 声明的**规则文件正文**（本书 9 个文件 18.8KB，上限 20KB，超限显式提示未注入项）；`MAX_DISCIPLINE` 12→16；`default_discipline()` 加 2 条文风/对撞纪律；纪律列表追加 `book.extra_discipline`（与 AGENTS.md §4 同源） |
| `scripts/state_sync.py` | `parse_block()` 返回重复键列表；重复键=问题（doctor 取首值、本脚本取末值，两个口径必须统一），`--fix` 可按「后值胜出」自动去重 |
| `scripts/regression_test.py` | 新增 2 个测试类共 9 例（`StyleEnforcementTests` 7 例含 2 例反向边界、`StateBlockHygieneTests` 2 例）；修 `counting_child()` 替身——记账逻辑移入 `if __name__ == '__main__'`，因为 preflight 现在会 import consistency_check 复用判据，把 import 算成一次执行会让「一次保存只跑一遍扫描」误报 |

### 文档同步（「代码改了文档没改」是本库明令禁止的）
`SKILL.md` B6 第 4 步与硬性纪律 3、`assets/AGENTS.template.md` 第 5.7 步/四步留痕纪律/2-3-4 步说明、
`docs/06-正文.md` 第 7 步、`docs/08-防漂移工具.md`（顺带修正原文「跳过降为警告」与实际拦截不符的错误）、
`templates/now.md`。全部写明：格式合法 ≠ 真跑过，留痕会被机检残留对撞。

### book.json 侧（唯一随书配置）
- `book.extra_discipline`：5 条本书红线（面板不下结论 / 条目是账不是文≤10行 / 禁机械时间戳 /
  林澈口癖「一切归账」/ 人味锚点）
- `checks.panel`：`{prefix:"【", max_lines:10, forbid:[这意味着,真相是,原来,其实,实质是]}`
- `checks.rhythm`：**由 null 改为启用**。阈值为 2026-09-20 按本书 69 章实测分布取的临时值
  （18 / 18% / 35% / 55% / 1.5，词表沿用插件默认），目标值（模板默认 22/25%/25%/7.0）实测 0/69 可达，
  记录在 `story-style.md §3.1` 作为后续上调方向。**待拆样板书后校准。**
- `.soloent/rules/story-style.md` 与 `.soloent/constitution/MASTER.md`：✏️/待填全部填实
  （题材定位、人称视角、句长节奏表、清单体禁令三类高危场景、语言、六条人味锚点清单、六条红线、
  角色口癖四条）。MASTER §5 明确「宪法层三条 + 流程层两条」，细则指向 story-style 避免双份。
- now.md 状态块去重（10 份 chapter 键 → 1 份）；重跑 `sync.py` 让 AGENTS.md 带上新红线。

### 验证结果
- `regression_test.py` **54 例全绿**（原 45 + 新增 9）；`selftest.py` 24 项全过；`audit.py` 无问题；py_compile 通过。
- 本书体检：`doctor.py` **24 通过 / 0 警告 / 0 错误**——「now.md 写第 60 章」的旧警告随去重消失。
- 新闸门对本书 69 章的拦截面：**拦截 43 章 / 通过 26 章**；其中机检残留命中 55 处
  （黑名单 67 + 句式 50 + 面板 12 + 节奏…），全部**精确到行号**。
- 端到端实证（临时副本，不动原书）：`preflight --chapter 1` → 退出码 1，
  拦截信息为「第 1 章留痕宣称自查已完成，但机检仍可见 6 处去 AI 味残留
  （ch-1.md 第17行 AI句式/黑名单词「那一刻」；…第17行 [时间切片]…；第105行「轻轻」 等 6 处）」。
- 新增严重项 57 处的来源已核对：**全部来自新启用的句长节奏机检**，无一条是正典/人名/时间线误报。

### 遗留（已告知用户，未做）
- 43 章需要返修；本轮只把「闸门能看见并拦住」做到位，没有动任何正文。
- 节奏阈值为临时值，拆到样板书后需回调。

---

## 第二轮诊断：用户追问「这本书到底在说什么？一点新意都没有」

用户选择「边写边修」，同时质疑作品的新意。读完 `1-边界/预期.md`、`总纲与卷纲.md`、
`world/世界观.md`、`characters/人物卡.md`、69 章标题与 arc 后的结论：

### 病根同源
`预期.md` 写得不错（执念：「把最枯燥的记账写成最强金手指——纪律本身即外挂」；
对标《我有一座冒险屋》钩子密度＋《全球高武》数值纪律）。但 **`1-边界/` 里只有预期.md，
没有样板书、没有拆书成果**——对标停在口号层。缺少差异坐标时，生成端只能回归概率最高的写法。
这与文风失败是同一条因果：**配置层空着 → 生成取均值**。

### 已腐化的骨架文档（卷一之后失修）
- `outline/总纲与卷纲.md`（2521B）：卷纲只写到卷一（1–30），卷二标注「规划，本卷不写」——
  而正文已到 69 章、三卷细纲都在。**僵尸文档。**
- `characters/人物卡.md`（3392B）：只到 ch-30 的人物。卷二/卷三的
  苏映雪、簿房、白苇、掌柜、关海年**没有人物卡**——即兴长出来的。
- `world/世界观.md`（2347B）：有个极好的点子只有一句话——**「影兽＝灵气体系的坏账具象」**。

### 「没新意」的六条具体成因
1. 开局 10 章＝高武开局默认件套（废物体质→救人觉醒→武馆→考试季→比武台→猎兽赚钱→药剂→虚账）。
2. 金手指与系统流**同构**：每日复利≈签到、坏账眼≈洞察、第二页≈预支——**换了皮没换骨**。
3. 「纪律」从未成为代价：复利白拿，主角的偏执没有伤到他自己 → 抵消了「纪律即外挂」的原意。
4. 卷二起滑向最拥挤的赛道（查账人体系/收割派/守脉人/掌柜/灵脉/上古布局/父辈秘密）。
5. 反派全是制度性贪腐（站长/蛀虫/克扣/账当库存）——最安全也最没辨识度的冲突。
6. 最具辨识度的具体质感（角/分/期限/欠条/房租/月供）在中后段被「宇宙级会计学」抽象稀释。

### 被浪费的三个原创种子（书里已有，只是没长起来）
- **影兽＝坏账具象**（世界观一句话）→ 可抬成主线怪物体系：打怪＝对账，与现有 ch-14/27/29 同构，不用推翻剧情。
- **「账记在你名下，就得你去收」**（人物卡·弧光）→ 主题级装置：收账会伤人，主角要定「什么账可以不收」。
- **「开启总账者永久失去记账资格」**（canon 十三，ch-64 才出现）→ 金手指的代价，应提到卷一亮相。

### 待用户拍板（已提三方案 + 修复起点，未动正文）
按项目纪律「设定类拍板项一律先列建议与选项，agent 不得单方定案」+「整章重写禁止，
动笔前先列保留/迁移/新增/删除四列清单」，等作者选定方向后再动稿。
方案 A＝把代价装进金手指（小改）；B＝影兽抬成坏账怪物体系（中改，辨识度最高）；
C＝收账伤人变道德装置（主题级）。

**结果：用户放弃这本书（本就是测试用），转向「这个项目还有哪些没测到的地方」。**
正文一字未动（符合预期）。小说侧结论保留在此，将来另开书时直接用得上。

---

## 第三轮：全项目「未测项与盲区」盘点 → `docs/15-未测项与盲区清单.md`

### 盘点方法
脚本级覆盖率（回归/自检/审计三处引用统计）＋ 文档声明与代码实现逐条对照 ＋
对新建书做探针（`init_book` 在临时目录真建一本，看默认值）。

### 抓到的**新**问题（docs/14 未覆盖的三类）
1. **配置层空着也能开写**：`brief.py:140` 的 ✏️ 守卫**只管 canon.md**。
   `story-style.md`/`MASTER.md`/`预期.md` 无人检查。实测某书这两份与模板 byte 级一致，
   照样 doctor 24/0/0 写完 69 章。
2. **新建书默认＝三道文风防线全关**：`init_book` 写入 `checks.rhythm=null`、
   `checks.panel=None`、`extra_discipline=[]`，而两份风格模板仍是 ✏️
   → **下一本书必然重演本次事故**。
3. **第 7 步（`/review`）零执行、零校验**：69 章 `notes/` 里 review 产物数=0，
   且 `scripts/`+`hooks/` 里没有任何实现或校验。SKILL 说"跳过=只过一半的关"——那一半从没跑过。
4. **技能三连路径死链**（✅ 本轮已修）：AGENTS.template §3/§6 指向
   `{{vault}}/plugins-manifest.md` 与 `assets/plugins/`（**都不存在**，目录叫 `skills/`，13 个不是 30 个）。
   audit 的 B2 遇 `{{` 开头直接跳过 → 一直假绿。
5. **骨架文档腐化无人管**：某书人物卡停在 ch-30、总纲卷纲只到卷一且写着"卷二本卷不写"，
   正文已到 69 章。docs/14 把「正典双向一致性」列为**第三阶段建议**，从未实现。
6. **零测试组件 4 个**：`watch_and_check.py`（唯一无人值守入口）、`sync.py --all`、
   `install.py`、`bundle_vault.py`。其中 `_bundle.json` 记 **123 个技能**、实盘只有 **13 个**。
7. **拆书/样板书没做也能一路写下去**：NW-P1-005 修了"正典+细纲+账本"三件前置，
   **漏了「边界/拆书」**——而 `预期.md` 明写要对标两本书。

### 本轮顺手修掉的（都有反向验证）
| 项 | 改动 |
|---|---|
| AGENTS 模板死路径 + 写死数量 | `{{skills_dir}}/{{skills_count}}/{{skills_list}}/{{workflows_dir}}/{{workflows_count}}` 由 `sync.py` **数出来**；模板不再写死数字 |
| audit 两个覆盖洞 | 新增 **B0**（`{{vault}}` 路径必须存在）+ **A4**（AGENTS 模板占位符必须全被替换，用 example 真渲染一遍验证，不维护键名清单）；`STALE_PATHS` 增 `plugins-manifest.md`/`assets/plugins`/`_agent/` |
| kit.py 提示指向不存在的 `_agent/` | 改为「跑 init_book.py」+ `assets/book.example.json`（用计算出的绝对路径） |
| sync.py / selftest.py 文档串里的 `_agent/` 用法示例 | 改为 `<插件>/scripts/...` |
| 我自己引入的隐患 | `inject_canon.collect_rules()` 原先**静默跳过**读不到的规则文件；改为在注入正文里列出「未能注入」清单（包括超预算被截断的） |

### 验证
`regression_test.py` **54/54 全绿**；`selftest.py` 24/24；`audit.py` 无问题；py_compile 通过。
audit 反向验证（临时副本）：注入不存在路径 → B0 报；注入打错占位符名 → A4 报；干净副本 → 不报。

### 建议的下一步（已写进 docs/15，等用户挑）
1. `init_book` 写入可用默认 + `doctor` 增「写作前置体检」（**唯一能阻止下一本书重演**，不碰正文）；
2. 把 `/review` 产物纳入闸门校验；
3. watcher / `--all` 各补最小用例；
4. 新增「专名登记」检查（正文出现但 canon/人物卡里没有的名字 → 报未登记）。

---

## 第四轮：docs/15 逐项落地（用户：「把所有问题都修复了，然后继续看哪些是没生效的」）

### 已修（kit 侧）
| 项 | 改动 |
|---|---|
| 风格层无人查 | `kit.style_doc_issues()`（**一处判据**）：查 story-style／MASTER／预期 的占位符 + 「与模板一字不差」；`brief` 拦死（block）、`doctor` 报警（warn，新增 `check_writing_readiness`） |
| 新书默认全关 | `init_book`：`rhythm` 写**启动档**（`_tier: bootstrap`，`RHYTHM_BOOTSTRAP`）、`panel`（仅 `system` 标签）、`extra_discipline` 起手三条（`STARTER_DISCIPLINE`） |
| **init 从不部署规则** | 新增 `sync.do_deploy(book)` 调用——新书 `.soloent/rules/` 原先只有 story-style.md，9 条规则一条没落地。**这是「新书规则不生效」最直接的一环** |
| `/review` 零校验 | `gate.review_required`（新书默认 true）+ `preflight.review_blocker()`：出第 N 章要求第 N-1 章评审产物；`--chapter N` 不受约束 |
| 内容层零机检 | `consistency_check` 三项（全按 book.json 开关）：`name_roster` 三方互校、`hook_check`、`foreshadow_check` |
| 打包账实不符 | `bundle_vault` 的 counts 改为**记实盘** + `planned_counts` 溯源；`_bundle.json` 修正；audit 新增 **E3** |
| 模板死路径 | AGENTS 模板改用 `{{skills_dir}}/{{skills_count}}`（sync 数出来）；audit 新增 **B0/B4(A4)** |
| 薄壳可移植 | `doctor` 逐壳校验 `KIT_TOOLS` 是否指向当前插件 |
| 注入预算 | `doctor` 按 `inject_canon` 的同一批常量体检 canon/§7§8/规则合计 |
| watcher / `--all` 零测试 | 各补真实用例（watcher 会启动→检测改动→留 watch tag 报告） |
| `--json` 隐式不推进校验点 | 已改：`--json` 只决定输出格式（自动入口本来就显式传 `--no-advance`） |
| audit 冒烟用例被打红 | 它造的章是「同一句复读 24 遍」，节奏机检开启后必然严重 ×2；换成正常叙述 |

### 第三轮「配了没人用」专项
- **`gate.batch_warn` 从没人读**（preflight 判据写死 `>1`，而 `--list-checks` 宣称阈值 5）→ 移除该键 + 文案改说真话
- **坏配置类型抛栈**：`checks.panel.max_lines="十"` → `int()` 崩，看不出哪个键错 → `kit.config_problems` 新增可选机检项类型校验（**接受小数**，0.5/7.0 合法）
- **`hooks.config.example.json` 漏了 `.codebuddy` schema**（本 harness 真正读的那份，2026-09-19 已实测「只配 zcode 则钩子永不触发」）→ 补说明
- **核实后不成立**：`templates/now.md` 的示例留痕行**不构成凭证**（被反引号包着，`FLOW_RE` 要求行首即 `[流程]`）——已写成用例钉住这个性质

### 被否决的方案（重要教训）
「从正文抽人名查未登记」实测在本书产出 **200+ 条全假**（`不知道`→「不知」、`也没回头`→「都没」）——
中文无词边界，滑窗必切碎常用词。**能机检 ≠ 该机检**；改用确定性三方互校
（canon 人物表 ／ `name_roster.registered` ／ `characters/*.md`），精准点名本书 8 个「有正文没登记」的角色。

### 验证
`regression_test.py` **68 例全绿**（原 54 → 65 → 68）；`selftest.py` 24/24；`audit.py` 无问题；全量 `py_compile` 通过。
本书 `doctor.py` 25 通过 / 1 警告（警告 = book.json 缺新基线键，属迁移提示）/ 0 错误。
本书机检分布：严重 57 · 中等 14 · 轻微 137（新增的钩子 16 + 人物卡/正典 2）。

### 仍挂着（已写进 docs/15 末尾）
`doctor --lib`、**拆书纳入 brief 前置**（唯一能防「写出没新意的书」的上游闸门）、
情绪落点/口癖落地、`install.py` 用例、E2 扫描范围、节奏阈值待样板书校准。

---

## 第五轮：把「还没做的」清完（用户：「不是说了全部做完么」）

| 项 | 结果 |
|---|---|
| **拆书纳入前置**（#14） | `kit.boundary_issues()`：**预期声明了对标作品却没拆书 → `brief` 拦截**（`1-边界/` 需有 `1.1_…` 产物）；没声明对标只提醒；`boundary.opt_out_reason` 有内容即放行（拍板留档过）。`doctor` 同步报警。**这是最上游的一道闸门**——文风管「写得像不像人」，拆书管「凭什么跟别人不一样」 |
| **节奏阈值校准** | 新增 `--suggest-rhythm [--from <目录>]`（**只读**）：按实测算分位 + 给出可粘贴进 book.json 的取值；并把算法抽成 `rhythm_stats()` 让机检与建议**共用同一套口径**（杜绝「按建议填了还是被拦」）。对话占比用 P90 而非 P75（它是风格选择不是质量线） |
| **E2 扫描范围** | 从 `assets/` 扩到 `assets/ + scripts/ + hooks/ + docs/`；用 `EXPLAIN_OK` 正则区分「历史说明」（含 早已/上一代/旧版/→ 等）与「真让人照做的死路径」。**扩了立刻抓到一处真问题**（见下） |
| **`install.py` 用例**（#8） | `--list` / `--dir` 装完 `--check` 一致 / 改动副本后 `--check` **必须报差异** |
| **`doctor --lib`**（#9） | 补用例时发现**更严重的真 bug**：坏配置走 `sys.exit(2)`，而 `except Exception` **接不住 `SystemExit`** → 库级体检遇第一本坏书就整体中止（恰恰在最需要它时没用）。改为逐本先自检配置 |
| **情绪落点／口癖**（#17/#18） | 判断类**故意不投机检**，改为写进 `/review` 的**必答清单**（`chapter-review.md` 第六步 6 项：情绪落点在哪一句／一笔账+一次兑现／口癖引原句／两处毛边／钩子与细纲是否一致／最像 AI 的一句）；并给第三步加「口癖落地／声音可分／人味毛边」。评审产物已被闸门校验 → 「必答」才真的成立 |

### 第五轮新抓到的（扩展 E2 后立刻见效）
- **`assets/book.example.json` 的 `_readme` 教人跑 `python _agent/sync.py`** —— 指向早已不存在的目录，
  而这段 `_readme` 会**复制进每一本新书的 `book.json`**。已修正。（同类死路径此前只在 assets/ 里查，所以漏了）
- **`assets/skills/wuhang-long-all/4-正文/SKILL.md` 写着「新增 ≥5 章未过闸」** —— 又是那条作废的 batch_warn 阈值；
  技能文件是写作 agent 会读到的东西。已改为「≥2 章」并注明旧说法作废。
- **`doctor --lib` 的 SystemExit 中止**（见上表）。
- 顺带消除 `docs/15` 自身的内部矛盾（第二轮表说「E2 仍只有 assets/」，与第四轮实际不符）。
- 一处**自己造成的差点翻车**：改 `book.example.json` 的 `_readme` 时只写回 4 条、丢了 3 条有用说明，
  复查时发现并补回（现 8 条）。

### 验证（最终）
`regression_test.py` **76 例全绿**；`selftest.py` 24/24；`audit.py` 无问题（含扩展后的 E2）；全量 `py_compile` 通过。
本书 `doctor.py`：25 通过 / 2 警告（`book.json` 缺 3 个新基线键＝迁移提示 + 写作边界未就绪）/ 0 错误。

### 现在真的只剩两类（都不阻塞）
1. **需要人拍板的数值**：新书 `checks.rhythm` 的启动档要收敛到目标档（工具已能给建议值）；
   存量书跑一次 `migrate_config.py` 补基线键即可。
2. **故意不做机检的判断类**：情绪落点、人味、声音可分——靠 `brief` 第六节 + `/review` 必答清单 + 闸门校验评审产物，
   三层但不是机检（做过的抽取式专名检查已证明会变成噪音源）。





