---
name: novel-writing
description: 中文长篇网文创作全流程。用于开新书、继续写某本书、执行 /init 或 /review、搭建世界观与力量体系、做角色设定、写总纲/卷纲/细纲、写章节正文、检查设定漂移或数值矛盾、拆解样板书、正文润色去 AI 味，或用户提到"卡文""续写""审查这章""换窗口""上下文太满"时。
license: 随附规则与技能文本源自用户自有资产（writing-vault）
agent_created: true
---

# 中文长篇网文创作

## 这个技能做什么

把「写一本长篇网文」从模糊的对话变成一条有闸门的流水线。它自带两样东西：

1. **规则与技能库**（`assets/rules/`、`assets/skills/`、`assets/workflows/`）——
   去 AI 味规则、题材风格规则、标点/润色/节奏三连技能、拆书与设定阶段技能。
   全部随插件分发，不依赖外部路径。
2. **防漂移工具**（`scripts/`）——写前接地单、单章对账、跨章账本、状态同步、出章闸门、后台监视。
   工具的检查项由每本书的 `.soloent/book.json` 驱动，代码里不含任何一本书的内容。

## 与 SoloEnt 生态的对应关系

本项目的方法论与文件约定沿用 **SoloEnt（灵蟹创作）** 那一套。对齐点如下：

### 四个内置命令

沿用 SoloEnt 约定。用户输入这些命令时按对应流程处理：

| 命令 | 作用 | 你该做什么 |
|---|---|---|
| **`/init`** | 生成 / 补建 `SOLOENT.md` | **不是"开新书"**。读 `chapters/` `outline/` `world/` `characters/` 归纳出 8 板块面板（摘要 + 指针）；任意阶段都可跑 |
| **`/compact`** | 压缩会话上下文 | **先落盘再压缩**：`now.md` + `SOLOENT.md §7/§8` + 账本行更新完，才谈压缩 |
| **`/review`** | 章节质量**五层**审查 | 读 `assets/workflows/chapter-review.md`；产出 `notes/review-chapter-{章号}-{日期}.md`。**先过闸门再评审** |
| **`/market`** | 发行 / 营销策略 | 读 `assets/workflows/publishing-diagnosis.md`；产出 `notes/marketing-plan-{日期}.md` |

> 开新书 ≠ `/init`。开新书是 `init_book.py`（见场景 A），`/init` 只是生成中央面板。
> 完整说明见 `docs/12-命令与操作.md`。

### 两类审查，顺序不能反

| | 管什么 | 性质 |
|---|---|---|
| `tools/preflight.py` | 正典冲突、数值、章号、句长分布、标点 | **机检**，失败关闭 |
| `/review` | 追读性、钩子质量、角色声音、AI 味 | **评审**，需要判断 |

**先过闸门（硬伤清零），再跑 `/review`（质量评审）。** 反了就是在有硬伤的稿子上做质量评估。

### 三类内容的触发方式

| 类型 | 位置 | 怎么触发 |
|---|---|---|
| **Rule** | `assets/rules/` | **安装后自动生效**，无需手动调用。由 `book.json` 的 `rules.load` 决定加载哪几条 |
| **Workflow** | `assets/workflows/` | **用命令/点名触发**（如「走一遍三层大纲」） |
| **Skill** | `assets/skills/` | **按场景自动触发**（描述匹配到就用） |

### 核心文件：SOLOENT.md

**`SOLOENT.md` 是每本书的中央控制面板**（8 大板块，AI 的长期记忆，Single Source of Truth）。
它不是可选项——长篇写作的上下文漂移问题靠它解决。

```
SOLOENT.md                    ← 中央面板：摘要 + 指针
├── §1 项目 DNA              → book.json 的 book 段（机器）
├── §2 世界系统              → world/*.md（指针）
├── §3 角色                  → characters/*.md（指针）
├── §4 情节与结构            → outline/*.md（指针）
├── §5 风格指南              → .soloent/rules/story-style.md
├── §6 关键约束              → .soloent/canon.md（值）+ book.json 的 checks（机器）
├── §7 活跃写作状态          → .soloent/memory/now.md（详细版）
└── §8 路线图                → .soloent/memory/roadmap.md（详细版）
```

**铁律**：摘要留 SOLOENT.md，细节存分档。**不要把世界观正文粘进 SOLOENT.md。**

### 规划模式 / 行动模式

| 场景 | 该用哪个 |
|---|---|
| 开新书、想法模糊、情节漏洞、重大结构变更 | **先出计划等作者确认**（规划模式）——不要直接改文件 |
| 日常写作、任务明确（「写第 31 章」「把主角改名」） | **直接执行**（行动模式） |

在多数 harness 里这对应「Plan / Act」模式选择器。**如果你处于规划模式，只输出计划、不写文件。**

### 会话与上下文（长篇写作的隐形杀手）

长篇崩掉的原因，一半是设定漂移，另一半是**上下文爆了还不换窗口**。

- **上下文不是越大越好**。建议上限控制在 **200K–300K**，别用满 1M——
  过大的窗口会让注意力分散，反复探索修改时性能反而下降。
- **两条阈值**：到 **100–150K** 就该在合适时机开新窗口；超过 **约 200K** 可能成本激增
  （部分模型按会话大小阶梯收费，且缓存跨天会失效）。
- **任务隔离原则**：一个会话只做一件事。「每章一停」本身就是天然的隔离边界。
  拆书、卷末大改、设定讨论——各自单独开窗口。
- **`/compact`**：会话堆积过多时压缩为概要。**但它不能替代落盘**——
  压缩是会话层的，`SOLOENT.md` / `now.md` 是文件层的事实。**先落盘，再压缩。**
- **开新窗口前必做交接**：把状态落盘（`now.md` + `SOLOENT.md §7/§8` + 账本）、
  跑一次 `preflight.py` 确认绿、把讨论结论写进文件。

> 完整做法见 `docs/11-会话与上下文.md`。
> 一句话：**文件是事实，会话是缓存。** 缓存可以清、可以压缩、可以换——只要事实在文件里。

### 用户说「换窗口 / 上下文太满 / compact」时

1. 先**落盘**：更新 `now.md`、同步 `SOLOENT.md §7/§8`、补账本行；
2. 跑 `python tools/preflight.py`，确认严重为 0；
3. 把本次会话里**还没写进文件的结论**整理成 `notes/` 下的文档（或并进 SOLOENT.md 对应板块）；
4. 输出一份**交接摘要**给用户，让他贴进新窗口——或者直接告诉他：
   新窗口里说「继续写《X》」即可，恢复协议会自动读全所需信息。

**不要只在会话里说"记住了"。** 会话结束就没了。

## 判断用户要做什么

先分场景，**不要一上来就跑工具**：

| 用户说 | 走哪个场景 |
|---|---|
| 「我想写本小说」「开新书」「从零开始」 | **场景 A：开新书** |
| 「继续写」「写第 N 章」「接着上次」 | **场景 B：继续写** |
| **`/init`**（或「补一下 SOLOENT.md」「面板过时了」） | **场景 D：生成/补建中央面板** |
| 「帮我做世界观/设定/大纲/细纲」 | **场景 C：单步任务** |
| 「这章有没有问题」「检查一下设定」 | **场景 C：单步任务**（跑闸门） |
| **`/review`**（或「审查这一章」） | **场景 C** → 走 `/review` 五层 |
| **`/market`**（或「怎么发行」） | **场景 C** → 走 `/market` |
| **`/compact`**（或「上下文太满」「换窗口」） | 先落盘，再压缩（见下） |

**判断当前在哪本书**：从当前工作目录逐级向上找 `.soloent/book.json`。找到就是那本；
找不到再问用户。**不要凭猜测选书。**

---

## 场景 A：开新书（对话式初始化）

> ⚠️ **开新书 ≠ `/init`。** 本场景建的是**整个项目**（目录 + 配置 + 工具 + 中央面板）。
> `/init` 只生成 `SOLOENT.md`，见场景 D。

### A1. 先问，一次问完

用 AskUserQuestion 一次性收集下面这些。**不要一个问题一条消息地挤牙膏。**

| 要问的 | 说明 | 默认 |
|---|---|---|
| 书名 | 可先留「待定」，后面再改 | 取目录名 |
| 小说文件夹放哪 | 建议 `<作品库>/<书名>/` | 由用户给 |
| 题材 | 自由描述，如「都市高武 · 校园学院流」 | — |
| **题材标签** | 决定加载哪几条风格规则，见下表 | 按题材推断后让用户确认 |
| 平台 | 番茄男频 / 起点 / 其他 | 番茄男频 |
| 单章字数 | 通常 2000–3000 | 2400 |
| 篇幅目标 | 如「100 万字以上」 | — |
| 有没有样板书 | 有样板书 → 建议先走拆书（场景 C-1） | 无 |

**题材标签 → 规则**（可多选叠加，这是"按风格自动触发"的实现方式）：

| 标签 | 适用 |
|---|---|
| `urban` | 现代都市 / 校园 / 职场等现实背景 |
| `xianxia` | 修真 / 仙侠 / 东方玄幻 |
| `system` | 主角有系统 / 面板 / 数值化金手指 |
| `farming` | 经营 / 建设 / 种田流 |
| `creative` | 设定驱动 / 一文一大脑洞 |

通用 7 条（系统级 2 + 去 AI 味 4 + 网文基础 1）**所有书都加载**，不用问。

### A2. 一条命令生成全部

```bash
python "<插件目录>/scripts/init_book.py" \
  --dir "<作品库>/<书名>" \
  --title "<书名>" \
  --genre "<题材描述>" \
  --platform "<平台>" \
  --tags <标签,逗号分隔> \
  --words 2400 \
  --length "<篇幅目标>"
```

它会建好目录骨架、写好 `book.json`、**生成 `SOLOENT.md` 与四个记忆文件**、
复制其余骨架模板、**并生成 `AGENTS.md` 与 `tools/` 薄壳**。加 `--json` 可拿结构化摘要。

### A3. 报告并给出下一步选择

生成完**必须停下**，向用户报告「建了什么」，然后让他选：

> 书已经建好了。接下来建议先做哪一步？
> **A. 拆样板书** —— 有对标作品的话，先拆它的开篇、节奏、文风，产出可执行的写作边界（推荐）
>   ⚠️ 若 `1-边界/预期.md` 里**声明了对标作品**却没做拆书，`brief.py` 会拦下每一章
>   （判据：`1-边界/` 下要有 `1.1_开篇拆解.md` 这类产物）。真不需要拆的，在 `book.json` 的
>   `boundary.opt_out_reason` 写明理由即可放行——**要的是一次明确决定，不是硬卡**。
> **B. 先做世界观** —— 没有样板书，或者设定已经想清楚了，直接从世界观与力量体系开始
> **C. 先写探索草稿** —— 已有想法，边写边补设定
>
> 试写走 `tools/draft.py --name "<草稿名>"`：草稿落在 `drafts/`，头部标记
> **NON-CANONICAL／非正典**。**禁止**把试写稿直接当正式第 1 章写进 `chapters/`——
> 正式章节要求正典、细纲、账本行就绪（缺任一项 `brief.py` 都会拦截）。
> 草稿转正的清单在草稿文件头；补齐前置资料并经作者确认后，另存为 `chapters/ch-N.md`
> 走正式流程过闸。

**不要替用户决定。** 这三个入口对应 `docs/` 下不同的手册。

### A4. 后续阶段

按 `docs/00-总览与流程.md` 的流程地图推进，每个阶段完成后停下等确认。
**每定一条设定就落盘到对应文件，并同步进 `SOLOENT.md` 的索引**——别只聊不写。

> ★ **第二条铁律：每阶段开始前先回看 `1-边界/预期.md`。**
> 它是开新书时填的"你自己的声音"（题材边界、不想写什么、核心爽点、为什么写这本）。
> 防的是**越写越偏**——卡文时第一件事不是找灵感，是回看它。
> 详见 `docs/02-拆书与边界.md`。

---

## 场景 B：继续写（每章循环）

> **本循环 ＝ 武行全流程的第六步（`4-正文`）＋ 第七步（`5-审查`）。**
> 全流程定义见 `assets/skills/wuhang-long-all/SKILL.md`。对应关系：
> - `4-正文` 步骤 3「完稿后校准（五项检查）」→ 下面的 **B6 ＋ B7**
> - `5-审查` 模块 3「正文质检」→ 下面的 **B7**
>
> **编号对照（别和 `docs/06-正文.md` 的步骤号搞混）**：
> 本节用 `B0…B8`，其中 `B0` 是「定位与恢复」——`docs/06` 没有这一步；
> `docs/06` 用 `0…8`。**两者相差 1 位**：本节 `B2`（起草）＝ `docs/06` 的**步骤 1**。
> **冲突时以 `docs/06-正文.md` 的步骤号为准**，本节只是执行视角的展开。
>
> **两者都不许省**——只过闸门，正文就只过了一半的关。

### B0. 定位与恢复

1. 从工作目录向上找 `.soloent/book.json`，确认是哪本书。
2. 读 `SOLOENT.md`（**中央面板：身份、约束、全部文件指针**）。
3. 读 `.soloent/canon.md`（**正典速查表，关键值一律从此取，不凭印象**）。
4. 读 `.soloent/memory/now.md` 的状态块与叙述区。
5. **口述复述当前状态与本章任务，再动笔。**

### B1. 写前接地（强制）

```bash
python tools/brief.py          # 下一章；也可 brief.py 31 指定章号
```

它会把「上一章末的准确状态 + 本章细纲节拍 + 待接伏笔 + 在场角色 + 硬性纪律」一次打出来。
**目的不是省事，是把「凭印象写」换成「照着事实写」。**

### B2–B5. 起草 → 自查 → 三连 → 复查（**四步全部强制**）

对应 `docs/06-正文.md` 的步骤 1–4。**四步都要跑，一步都不许跳**；跑完在 B6 留痕（闸门会校验）。

**B2 起草**：按细纲节拍码字，单章字数服从 `book.json` 的设定。

**B3 句法自查**：按 `assets/rules/ai-anti-patterns.md` 词表**逐条过一遍**，违规即改。
同时对照 `story-style.md` 的「人味」条款问一句：**这一章有没有两处"没用"的东西？**
（不推动情节、不交代信息，纯粹是人的毛边——这是「像人写的」最直接的抓手）

**B4 技能三连**：按顺序跑，**读每个技能的 SKILL.md 按说明执行**，不要凭名字猜它做什么：

| 顺序 | 技能 | 管什么 |
|---|---|---|
| 1 | `chinese-punctuation` | 中文标点规范（含脚本 `scripts/fix_punctuation.py`） |
| 2 | `humanizer-webnovel-cn` | 真人味润色（网文男频版） |
| 3 | `pacing-control` | 节奏控制 |

**B5 连续性复查**：对照 `world/`、`characters/`、`outline/伏笔回收总表.md`、记忆文件逐项核对。
**发现冲突先报告，不要自行改设定。**

> **为什么这四步必须强制**：闸门只能验「算得出来的东西」（人名／刻度／字数／账本／黑名单词），
> **验不了 AI 句式结构、人味、钩子质量**。2026-09-18 的 ch-31、ch-32 两次漏跑，
> 作者当场反馈「AI 味重、不像人写的」——不是模型变差，是这一层没跑。

### B6. 收尾回写 + 出章闸门（强制）

1. 更新 `.soloent/memory/now.md` 状态块与五个小节（当前状态/人物快照/近期情节流/待处理线索/执行评审）；
2. 把 `SOLOENT.md §7` 的摘要同步过来；
3. 在 `.soloent/ledger.tsv` **追加一行**（列序见 `book.json` 的 `ledger.columns`）；
4. **在 `now.md` 的「流程留痕」小节追加一行**（第 2／3／4／5 步：自查／三连／复查／回写 的执行凭证），格式：
   `[流程] ch-N 自查X 三连Y 复查Z 回写OK`
   X／Y／Z ＝ 该步**发现并修复**的处数。干净章写 0 是正常的，但必须是真跑过的；
5. 跑闸门：

```bash
python tools/preflight.py      # 退出码 0 才许写下一章
```

> ⚠️ **留痕数字会被对撞**（2026-09-20 复盘）：闸门不仅校验那行的**格式**，
> 还会把该章的**机检残留**（黑名单词／7 类句式／同段连发／比喻堆砌／章末升华／面板条目）
> 与「自查已完成」这个声明对撞。**留有残留又没写明理由 = 直接拦截**——
> 因为真跑过 B3 句法自查的章，机检看得见的那几类应当已经清零。
> 编数字过不了闸：`自查0 三连0 复查0` 只是格式合法，不再是通行证。
>
> **确有语境例外要保留**（伏笔位比喻、角色专属细节）：在行尾写明理由——
> `[流程] ch-N 自查X 三连Y 复查Z 回写OK；保留：<理由>`，闸门降为警告，/review 逐条复核。

**不通过就停下修，禁止「先写着后面再改」。**

> 闸门会校验第 4 步那一行——**没有留痕 = 直接拦下**。
> 它拦不住"糊弄"（机器验不了你改得多认真），但拦得住"忘了跑"——
> 而 2026-09-18 的两次翻车，恰恰都是忘了跑。

### B7. 质量审查（强制，不可跳过）

> **闸门只管算得出来的东西**——人名、年份、刻度、字数、23 个黑名单词。
> **AI 句式结构、人味、钩子质量，机检天生看不见。** 这一层只能靠本步。
> 顺序不能反：**先过闸门清硬伤（B6），再做质量评审（B7）。**
> ⚠️ **本步不是"靠自觉"**：闸门会校验**上一章**的评审产物
> （`notes/review-chapter-<N-1>-<日期>.md`）——出第 N 章时若它不存在，直接拦截。
> 2026-09-20 复盘：某书 69 章的评审产物数是 **0**，而此前没有任何检查发现。

两件事都要做，缺一不可：

**① 按 `assets/rules/ai-anti-patterns.md` 逐条扫（7 类句式 + 词级）**

| 类别 | 禁项 |
|---|---|
| 句式 | 否定排比（「不是A，不是B，是C」）／时间切片（一瞬间·刹那·这一刻）／「一股·一丝·某种」+抽象情绪／升级式升华／**「他知道」裁判腔**／排比三联（「他想起A，他想起B」）／情绪定语化／推拉连词垫字（「还没等他……」） |
| 词级 | 忽然·突然·猛地（**一页 ≥2 次即违规**）／柔化副词（缓缓·微微·轻轻·悄悄·默默）／生理模板（深吸一口气·心头一紧·眼眶一热）／**比喻（每章 ≤2 个）**／眼睛文学（眸子·眼底划过）／空气时间拟态（空气凝固·时间静止）／复合修饰（「A，却带着B」） |

> **判据：一页内同类句式出现 ≥2 次即判违规。**
> 伏笔位比喻、角色专属细节可保留，但须在审查时注明理由。

**② 走 `5-审查` 模块 3（正文质检）**

读 `assets/skills/wuhang-long-all/5-审查/SKILL.md` 的**模块 3**，
模拟**毒舌读者／资深编辑**视角做段落级批注：

- 沉浸感：是否用动作、对话、细节代替了设定说明？五感是否调动？
- 情绪调动：读完是爽、是期待，还是憋屈？（**憋屈不能过夜**）
- 文风红线：翻译腔、重复词、过度书面语、违反 `MASTER.md` 禁忌
- 重点标「出戏／啰嗦／逻辑不通」，**并给改写示范**

**发现即改，改完重跑 B6 的闸门**（改了正文就要重新过闸）。

> 这一步对应**武行全流程的第七步**（每章或每卷后跑 `5-审查`）。
> 跳过它，正文就只过了机检那一半的关——**AI 句式会整片漏过去**。

### B8. 停下等确认

每章完成后向作者汇报并等待确认或修改意见。**禁止连续生成多章**——
本库历史事故：19 章在 69 分钟内连发，从第 15 章起设定全线漂移。

---

## 场景 C：单步任务

读 `docs/` 下对应手册再执行：

| 任务 | 手册 |
|---|---|
| 拆解样板书、定写作边界 | `docs/02-拆书与边界.md` |
| 校准句长节奏阈值 | `python tools/consistency_check.py --suggest-rhythm`（对着样板书跑更准） |
| 世界观、力量体系、金手指 | `docs/03-世界观与设定.md` |
| 角色设定、冲突设计 | `docs/03-世界观与设定.md` |
| 总纲 / 卷纲 | `docs/04-大纲.md` |
| 章纲 / 细纲 | `docs/05-细纲.md` |
| 写正文 | `docs/06-正文.md` |
| 审查、润色、修订 | `docs/07-审查与修订.md` |
| **开篇审核（黄金三章）** | `assets/skills/golden-three-chapters-review/SKILL.md`（资深责编视角，10 维度 10 分制评分 + 修改意见） |
| 设定漂移、数值矛盾 | `docs/08-防漂移工具.md` |
| 规则加载、禁载哪些 | `docs/09-规则清单.md` |
| 装到别的 harness | `docs/10-安装与多harness.md` |
| 换窗口、上下文太满、会话交接 | `docs/11-会话与上下文.md` |
| 命令、Plan/Act、省 token 操作、设置项 | `docs/12-命令与操作.md` |
| Git 版本控制、回退 | `docs/13-版本控制.md` |
| 卡文、脑暴 | 用 `assets/workflows/brainstorming.md` |

---

## 场景 D：`/init` —— 生成 / 补建中央面板

> **这不是"开新书"。** 它只处理 `SOLOENT.md` 一个文件，而且**项目任意阶段都能跑**。

**两种情形**：

**D1. 新书刚建好**（`init_book.py` 已产出 `SOLOENT.md` 骨架）
→ 不是重新生成，而是**在商讨过程中逐步填充**。每定一条设定就并进对应板块，不必一次填满。

**D2. 已有内容但面板缺失或过时**
→ 读 `chapters/`、`outline/`、`world/`、`characters/`、`.soloent/`，
按 8 板块归纳出 `SOLOENT.md`：**摘要 + 指针，不要把正文粘进去**。
§7 从 `.soloent/memory/now.md` 取，§8 从 `roadmap.md` 取。

**判据**：`python tools/state_sync.py` 会检查 §7 是否滞后。
报「SOLOENT.md §7 滞后」就是该跑 `/init`（或手工同步）了。

**短篇可以不用**：官方明确篇幅较短的项目可以不初始化。骨架里生成了放着即可，不影响逐章流程。

---

## 工具速查

| 工具 / 命令 | 作用 | 何时跑 |
|---|---|---|
| `scripts/init_book.py` | 新书初始化（对话式，含 SOLOENT.md） | 开新书 |
| **`/init`** | 生成/补建 `SOLOENT.md` | 任意阶段 |
| `tools/brief.py` | 写前接地单 | **写前** |
| `tools/consistency_check.py` | 单章对账（**机检项清单以 `--list-checks` 为唯一来源**）；`--suggest-rhythm [--from <样板书目录>]` 按实测给出可粘贴的句长节奏阈值（只读） | 写后 / 校准阈值 |
| `tools/state_sync.py` | 状态同步校验（含 SOLOENT §7 滞后检查） | 写后（闸门内置） |
| `tools/ledger.py` | 跨章账本对账 | 写后（闸门内置） |
| `tools/preflight.py` | 出章闸门（汇总以上） | **写后，强制** |
| **`/review`** | 章节质量**五层**审查 | **每章后**（在闸门之后） |
| **`/market`** | 发行 / 营销策略 | 手稿完成时 |
| **`/compact`** | 压缩会话上下文 | 上下文 100K+（先落盘） |
| `tools/watch_and_check.py` | 后台监视，改动即自动过闸 | 可选常驻 |
| `tools/doctor.py` | 系统体检（只读） | **每次会话自动跑**；排查问题手动跑 |
| `scripts/sync.py` | 重生成 `AGENTS.md` 与薄壳 | 改完 `book.json` 或模板后 |
| `scripts/selftest.py` | 插件自检（**检查项会不会触发**） | 怀疑工具没生效时 |
| `scripts/audit.py` | 插件自审计（**模板/技能/文档/脚本对不对得上**） | **改过插件后** |

> **`doctor.py` 不用你记**：它挂在 SessionStart 钩子上自动跑，**只在有问题时才显示**，
> 全绿时完全不出现。想看全部细节再手动 `python tools/doctor.py`。

> `tools/` 下是**薄壳**，真身在 `<插件>/scripts/`。改逻辑改插件，改本书正典改 `book.json`。

## 硬性纪律（不可跳过）

1. **每章写完后必须过闸**：`python tools/preflight.py`，退出码 0 才许写下一章。
2. **过闸后必须做质量审查**（`assets/rules/ai-anti-patterns.md` 逐条扫 ＋ `5-审查` 模块 3）。
   闸门只管算得出来的东西；**AI 句式、人味、钩子质量全靠这一步**。
   跳过它 = 正文只过了一半的关。做法见场景 B 的 B7。
3. **每章第 2／3／4／5 步必须逐条执行，并在 `now.md` 留痕**（句法自查／技能三连／连续性复查／收尾回写）。
   留痕格式：`[流程] ch-N 自查X 三连Y 复查Z 回写OK`。**闸门会校验这一行，缺失即拦截**。
   B3–B5 是 AI 味与人味的唯一防线，机器验不了，只能靠留痕兜底。
   **并且闸门会把留痕数字与该章的机检残留对撞**：留有黑名单词／句式违规等机检可见的残留
   又没写明理由 = 拦截。**格式合法 ≠ 真跑过**——编数字不比不写安全。
   确需保留的违规处，在行尾加「；保留：<理由>」（降为警告，交 /review 复核）。
4. **每章完成后停下**等作者确认，**禁止连续生成多章**。
5. **整章重写禁止**。重设计 = 在旧稿上做外科手术；动笔前先列「保留 / 迁移 / 新增 / 删除」四列清单；
   已验证的文本资产（打磨过的台词、场景节奏、留白动作）**只迁移不删除**。
6. **单一来源**：改人物或数值口径，先改 `.soloent/canon.md`，再改 `.soloent/book.json` 里对应的机检项，
   最后同步 `SOLOENT.md §6`。
7. **摘要留 SOLOENT.md，细节存分档。** 不要把世界观正文粘进中央面板。
8. **每阶段开始前回看 `1-边界/预期.md`**（防越写越偏）。卡文时也先看它——
   很多"写不下去"其实是**偏了**，不是没灵感。
9. **章末禁令**：禁止说教与升华，必须收在悬念、动作或未尽对话上。
10. **标点**：中文全角；正文对话一律用中文双引号（“ ”），严禁直角引号「」。
11. **拍板留档**：骨架层变动先给选项等作者确认；确认后当天在 `notes/` 留档，未留档的变动视为未发生。
    **设定类拍板项一律先列建议与选项，不得单方定案。**
12. **禁读心式直述**（「他心里想」「他知道」）与「一股 XX 涌上心头」式套路心理——
    心理活动靠动作、对话、生理反应带出来，不直接叙述。

## 环境

- **一律用 `python`，不要用 `python3`**（部分 Windows 环境的 `python3` 会解析到 Store 占位符）。
- 输出中文乱码时加 `PYTHONIOENCODING=utf-8`。
- 详细安装（多 harness）见 `docs/10-安装与多harness.md`。
